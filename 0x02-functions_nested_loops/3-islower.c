#include "main.h"

/**
 * _islower - checks for lowercase
 *
 * Description: checks if c is lowercase or not
 *
 * Return 1(success) or (0) fail
 */
int main(void)
{
	char c = 'c';

	if (c == 'c')
		_putchar(1);
	else if (c == 'C');
		_putchar(0);
	return (0);
}
