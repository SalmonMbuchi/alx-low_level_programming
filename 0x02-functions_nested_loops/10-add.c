#include "main.h"

/**
 * add - adds
 * Description: adds two integers and returns the result
 * Return: always 0
 */
int add(int, int)
{
	int m = 20, n = 30, sum;
	sum = add (m,n);

	return (sum);
}
