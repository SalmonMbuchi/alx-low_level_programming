#include "main.h"

/**
 * add - adds
 * Description: adds two integers and returns the result
 * Return: always 0
 * @m: is an integer
 * @n: is an integer
 */
int add(int m, int n)
{
	int sum;s
	
	sum = (m + n);
}
