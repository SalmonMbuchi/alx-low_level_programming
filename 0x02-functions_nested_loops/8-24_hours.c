#include "main.h"

/**
 * jack_bauer - prints every minute
 *
 * Description: prints every minute of the day 
 *
 * Return: 0(success)
 */
void jack_bauer(void)
{
	int r;

	for (r = 0; r <= 1439; r++)
	{
		int l;
		while (l > r++)
			_putchar(00:00);
	_putchar('\n');
	return ();
}

