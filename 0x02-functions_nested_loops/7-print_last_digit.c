#include "main.h"

/**
 * print_last_digit - prints
 * Description: prints the last letter of the alphabet
 *
 * Return: Always 0
 */
int print_last_digit(int)
{
	int q;
	int lastNum;

	lastNum = q % 10;
	_putchar(lastNum);

	return (0);
}
