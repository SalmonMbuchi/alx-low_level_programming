#include "main.h"

/**
 * more_numbers - prints a row of numbers ten times
 * Return: void
 */
void more_numbers(void)
{
	int c;

	for (c = 0; c <= 10; c++)
	{
		for (c = 0; c <= 14; c++)
		{
			_putchar(c);
		}
	_putchar(c);
	}
	_putchar('\n');
}
