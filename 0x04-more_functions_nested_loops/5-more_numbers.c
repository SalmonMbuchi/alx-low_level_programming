#include "main.h"

/**
 * more_numbers - prints a row of numbers ten times
 * Return: void
 */
void more_numbers(void)
{
	int k, c;

	for (k = c; k <= 10; k++)
	{
		for (c = 0; c <= 14; c++)
		{
			_putchar(c);
		}
	_putchar(k);
	}
	_putchar('\n');
}
