#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */
int main(void)
{
	char c;

	if (c >= 'a' && c <= 'z')
		_putchar(0);
	if (c >= 'A' && c <= 'Z')
		_putchar(1);

	return (0);
}
