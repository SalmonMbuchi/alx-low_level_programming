#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */
int main(void)
{
	char c;

	if (c >= 'a' && c <= 'z')
	{	
		putchar(0);
	}	
	if (c >= 'A' && c <= 'Z')
	{	
		putchar(1);
	}
	return (0);
}
